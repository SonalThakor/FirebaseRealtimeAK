<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.FirebaseRealtimeAK"
        tools:targetApi="31">
        <activity
            android:name=".activity.EmployeeDetailsActivity"
            android:label="EmployeeDetails"
            android:exported="false" />
        <activity
            android:name=".activity.FetchingActivity"
            android:exported="false"
            android:label="Fetch Data" />
        <activity
            android:name=".activity.InsertionActivity"
            android:exported="false"
            android:label="Insert Data" />
        <activity
            android:name=".activity.FirebaseActivity"
            android:exported="false"
            android:label="Firebase" />
        <activity
            android:name=".activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>